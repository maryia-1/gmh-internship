# gmh-internship
When running this project, you can recreate the project's dependencies by installing Node.js and running npm install, after which the Vite development server can be started using npm run dev.
